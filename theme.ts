import { createTheme } from "@mui/material/styles";

let myTheme = createTheme({
  palette: {
    primary: {
      main: "#0052cc",
      light: "#0052cc",
      dark: "#0052cc",
    },
    secondary: {
      main: "#edf2ff",
      light: "#edf2ff",
      dark: "#edf2ff",
    },
  },
});

export default myTheme;
